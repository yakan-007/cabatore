[phases.setup]
nixPkgs = ["python311"]

[phases.install]
cmds = ["cd ten-no-koe-backend && pip install -r requirements.txt"]

[start]
cmd = "cd ten-no-koe-backend && uvicorn main:app --host 0.0.0.0 --port $PORT"